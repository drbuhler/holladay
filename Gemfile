source "https://rubygems.org"

# Matches the GitHub Pages build environment.
gem "github-pages", group: :jekyll_plugins

# Needed for `bundle exec jekyll serve` on Ruby 3.x when previewing locally.
gem "webrick", "~> 1.8"
